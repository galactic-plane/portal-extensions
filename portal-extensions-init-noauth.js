// Portal Extensions Initialization (No Authentication Required)
// This file is auto-generated by the C# deployment plugin from manifest.json
// Only extensions that DO NOT require authentication are initialized here

// Wait for all extensions to load
document.addEventListener('portalExtensionsLoaded', function() {
    console.log('All non-authenticated portal extensions loaded successfully');
    
    // Extensions that require authentication should NOT be initialized here
    // They should be initialized in portal-extensions-init-auth.js instead
    
    // Add non-authenticated extension initializations below
    // Example:
    // if (typeof SomePublicExtension !== 'undefined') {
    //     SomePublicExtension.init({
    //         // configuration here
    //     });
    // }
});
